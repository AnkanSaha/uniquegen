"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function GenerateID(length) {
    let Numbers = [1, 2, 3, 4, 5, 6, 7, 8, 9, 0];
    let CountRound = length;
    let FinalID = [];
    while (CountRound > 0) {
        let TempRandomNumBer = Math.random() * (9 - 0) + 0;
        let FinalRandomNumber = TempRandomNumBer.toFixed();
        FinalID.push(Numbers[FinalRandomNumber]);
        CountRound--;
    }
    let FinalIDString = FinalID.join('');
    let FinalIDNumber = Number(FinalIDString);
    return FinalIDNumber;
}
exports.default = GenerateID;
